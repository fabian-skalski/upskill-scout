[tool.poetry]
name = "llm-service"
version = "0.1.0"
description = "LLM Service for Upskill Scout using Ollama"
authors = ["Your Name <you@example.com>"]
packages = [{include = "*", from="."}]

[tool.poetry.dependencies]
python = "^3.10"
fastapi = "^0.122.0"
uvicorn = "^0.38.0"
pydantic = "^2.12.5"
requests = "^2.32.5"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
